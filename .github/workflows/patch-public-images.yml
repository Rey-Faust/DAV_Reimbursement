name: Patch app hardening

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  already-patched:
    runs-on: ubuntu-latest
    steps:
      - name: Already patched directly
        run: echo "Auth and public visibility hardening now live in index.html."
