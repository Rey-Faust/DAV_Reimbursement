name: Patch public receipt images

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  already-patched:
    runs-on: ubuntu-latest
    steps:
      - name: Already patched directly
        run: echo "Public receipt image viewing is handled in index.html."
