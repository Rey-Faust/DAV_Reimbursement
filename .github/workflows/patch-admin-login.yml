name: Patch admin login email

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Already patched
        run: echo "Login settings and public visibility are handled directly in index.html."
