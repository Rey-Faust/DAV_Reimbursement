name: Patch admin login email

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Already patched
        run: echo "Admin login settings were patched in index.html."
